
<% if (lastPage > 1) { %>
  <nav aria-label="Pagination">
    <ul class="pagination">
      <li class="page-item">
        <a class="page-link" href="<%= '/' + pageName + '/?page=' + (currentPage - 1) %>">Previous</a>
      </li>
      <li class="page-item">
        <a class="page-link" href="<%= '/' + pageName + '/?page=' + 1 %>"><%= 1 %></a>
      </li>
      <li>...</li>

      <% if (lastPage > 2) { %>
        <% for(let p = Math.max(currentPage-3, 2);
                p <= Math.min(currentPage+3, lastPage-1);
                p+=1) { %>
          <li class="<%= 'page-item' + ( p === currentPage ? ' active' : '') %>">
            <a class="page-link" href="<%= '/' + pageName + '/?page=' + p %>"><%= p %></a>
          </li>
        <% } %>
      <% } %>

      <!-- <li class="page-item active" aria-current="page">
          <span class="page-link">2</span>
        </li>
        <li class="page-item"><a class="page-link" href="#">3</a></li>
     -->
      <li>...</li>
      <li class="page-item">
        <a class="page-link" href="<%= '/' + pageName + '/?page=' + lastPage %>"
          ><%= lastPage %></a
        >
      </li>
      <li class="page-item">
        <a class="page-link" href="<%= '/' + pageName + '/?page=' + (currentPage + 1) %>">Next</a>
      </li>
    </ul>
  </nav>
<% } else if (lastPage === 1) { %>
  <!-- Do not show anything if lastPage is 1 -->
  <nav aria-label="Pagination">
    <ul class="pagination">
      <li class="page-item">
        <a class="page-link" href="">1</a>
      </li>
    </ul>
  </nav>
<% } else { %>
  <div>No <%= pageName %> found</div>
<% } %>
